# PC Forge

A simple React application for browsing and sharing PC components.

> Course project for the ReactJS course (work in progress).

## Back-end

JSON Server + json-server-auth. The data is in `server/db.json`.

```bash
npm run server   # REST API on http://localhost:3030
```
